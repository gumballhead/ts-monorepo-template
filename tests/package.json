{
    "name": "tests",
    "version": "1.0.0",
    "scripts": {
        "api": "jest --projects api --runInBand --detectOpenHandles",
        "db": "jest --projects db --runInBand --detectOpenHandles",
        "test": "jest --projects api db --runInBand --detectOpenHandles",
        "start": "jest --projects api db --runInBand --detectOpenHandles --watchAll",
        "unit": "jest --runInBand --detectOpenHandles --projects ."
    },
    "dependencies": {
        "axios": "^0.21.1",
        "db": "workspace:db",
        "dotenv": "^8.2.0",
        "jest": "^26.6.3",
        "lib": "workspace:lib",
        "sequelize": "^6.5.0"
    },
    "devDependencies": {
        "ts-jest": "^26.4.4",
        "typescript": "^4.1.3"
    }
}
